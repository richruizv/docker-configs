# Netdata Cloud claim token (required to connect to Netdata Cloud)
NETDATA_CLAIM_TOKEN=
# Netdata Cloud base URL (default: https://app.netdata.cloud)
NETDATA_CLAIM_URL=https://app.netdata.cloud
# Comma-separated list of Room IDs to add the agent to (optional)
NETDATA_CLAIM_ROOMS=
